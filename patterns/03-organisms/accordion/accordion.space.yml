accordion:
  use: "@organisms/accordion/accordion.twig"
  label: Accordion
  description: Pure CSS collapsible accordion.
  fields:
    items:
      type: pattern
      label: Items
      preview:
        - id: accordion_item
        - id: accordion_item
        - id: accordion_item
  settings:
    container_width:
      type: select
      label: Container width
      default: default
      options:
        default: Full
        box_width_800: Box width - 800px
